$GatewayPython = 'E:\Deploy\Python\imap-gateway\.venv\Scripts\python.exe'
$AppDir = '위 출력의 AppDir'
$AsgiApp = '위 출력의 ASGI'

Set-Location $AppDir

& $GatewayPython `
    -m uvicorn `
    $AsgiApp `
    --app-dir $AppDir `
    --host 127.0.0.1 `
    --port 8000