$GatewaySource = 'E:\Deploy\Python\imap-gateway\source'
$GatewayRunBat = Join-Path $GatewaySource 'Run.bat'

Test-Path -LiteralPath $GatewayRunBat
Get-Content -LiteralPath $GatewayRunBat




Set-Location 'E:\Deploy\Python\imap-gateway\source'

& 'C:\Windows\System32\cmd.exe' `
    /d `
    /s `
    /c `
    'call "E:\Deploy\Python\imap-gateway\source\Run.bat"'



$GatewaySource = 'E:\Deploy\Python\imap-gateway\source'
$ConfigRoot = "$GatewaySource\src\imap_gateway"

Test-Path -LiteralPath $ConfigRoot

Get-ChildItem `
    -LiteralPath $ConfigRoot `
    -Force |
Select-Object Name, Mode, FullName


$GatewaySource = 'E:\Deploy\Python\imap-gateway\source'
$GatewayPython = 'E:\Deploy\Python\imap-gateway\.venv\Scripts\python.exe'

Set-Location $GatewaySource

& $GatewayPython -c "import imap_gateway; print(imap_gateway.__file__)"



$GatewaySource = 'E:\Deploy\Python\imap-gateway\source'
$GatewayPython = 'E:\Deploy\Python\imap-gateway\.venv\Scripts\python.exe'
$GatewaySrc = "$GatewaySource\src"

& $GatewayPython -c "import sys; sys.path.insert(0, r'$GatewaySrc'); import imap_gateway; print('python=' + sys.executable); print('module=' + str(imap_gateway.__file__))"




Test-Path "$GatewaySource\src\imap_gateway\config.py"
Test-Path "$GatewaySource\src\imap_gateway\__init__.py"

