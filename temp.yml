---
- hosts: windows
  gather_facts: false

  tasks:
    - import_tasks: roles/spring/tasks/context.yml
      tags:
        - always

    - import_tasks: roles/spring/tasks/backup.yml
      tags:
        - backup

    - import_tasks: roles/spring/tasks/clean.yml
      tags:
        - clean