$GatewaySource = 'E:\Deploy\Python\imap-gateway\source'

Get-ChildItem -LiteralPath "$GatewaySource\src" -Directory |
Select-Object Name, FullName

Get-ChildItem `
    -LiteralPath "$GatewaySource\src" `
    -Recurse `
    -File `
    -Filter 'main.py' |
Select-Object FullName