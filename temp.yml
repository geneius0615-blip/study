Get-NetTCPConnection -State Listen |
Where-Object {
    $_.OwningProcess -eq (
        Get-CimInstance Win32_Service -Filter "Name='GatewayService'"
    ).ProcessId
} |
Select-Object LocalAddress, LocalPort, OwningProcess