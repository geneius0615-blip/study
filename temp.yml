set "GCM_CREDENTIAL_STORE=dpapi"
git config --global credential.credentialStore dpapi

echo %GCM_CREDENTIAL_STORE%
git config --show-origin --show-scope --list | findstr /i "credentialstore"
where git
whoami