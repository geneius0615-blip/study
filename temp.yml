$oldPaths = @(
    'E:\Deploy\gateway',
    'E:\Deploy\Python\gateway',
    'E:\digital-twin-studio\imap\workplaces\gateway'
)

foreach ($oldPath in $oldPaths) {
    if (Test-Path -LiteralPath $oldPath) {
        Remove-Item `
          -LiteralPath $oldPath `
          -Recurse `
          -Force

        Write-Host "Removed: $oldPath"
    }
    else {
        Write-Host "Already absent: $oldPath"
    }
}