$env:PYTHONPATH = "$GatewaySource\src"

& $GatewayPython -c "import os, sys; print('PYTHONPATH=' + str(os.environ.get('PYTHONPATH'))); print(*sys.path, sep='`n'); import imap_gateway; print('module=' + str(imap_gateway.__file__))"