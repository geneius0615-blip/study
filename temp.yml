stage('Check Node') {
    steps {
        sh '''
            echo $PATH
            which node || true
            which npm || true
            node --version || true
            npm --version || true
        '''
    }
}