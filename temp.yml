$allYml = '.\group_vars\all.yml'

Get-Content $allYml |
ForEach-Object {
    if ($_ -match '^([A-Za-z_][A-Za-z0-9_]*):') {
        $matches[1]
    }
} |
Group-Object |
Where-Object Count -gt 1 |
Select-Object Name, Count