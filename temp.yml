- name: Check Tomcat port
  win_shell: |
    $connection = Get-NetTCPConnection `
        -LocalPort {{ tomcat.port }} `
        -State Listen `
        -ErrorAction Stop

    Write-Output $connection
  register: tomcat_port_debug
  changed_when: false