$ConfiguredEnvironment = (
    Get-ItemProperty `
        -LiteralPath $ParametersKey `
        -Name 'AppEnvironmentExtra'
).AppEnvironmentExtra

$ConfiguredEnvironment |
ForEach-Object {
    Write-Host "[$_]"
}